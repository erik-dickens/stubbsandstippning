# sv

To recreate this project with the same configuration:

```sh
# recreate this project
pnpm dlx sv@0.16.1 create --template minimal --types ts --add prettier eslint --install pnpm stubbsandstippning
```

## Drizzle

Använd port 5432 för npx drizzle-kit pull
